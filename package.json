{
  "name": "escape-rust-cobalt-city-worker",
  "version": "1.0.0",
  "description": "Cloudflare Worker for Escape Rust Cobalt City game",
  "main": "worker.js",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "game",
    "cloudflare",
    "worker",
    "kv"
  ],
  "author": "Your Name",
  "license": "MIT",
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}